<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.financial.manage.mapper.FilesMapper">
    <select id="getFilesListByIds" resultType="com.financial.manage.entity.Files">
        SELECT * FROM `mi_files` WHERE `del_flag` = 0
        <if test="ids != null and ids.size() > 0">
            AND `id` IN<foreach collection="ids" item="id" index="index" open="(" close=")" separator=",">#{id}</foreach>
        </if>
    </select>
</mapper>
